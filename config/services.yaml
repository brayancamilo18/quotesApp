parameters:
  app.campaign_active: "%env(bool:APP_CAMPAIGN_ACTIVE)%"

services:
  _defaults:
    autowire: true
    autoconfigure: true

  App\:
    resource: "../src/"
    exclude:
      - "../src/DependencyInjection/"
      - "../src/Entity/"
      - "../src/Kernel.php"

  # CampaignPolicy
  App\Domain\Quotation\Service\FixedDiscountCampaignPolicy:
    arguments:
      $active: "%app.campaign_active%"
      $discountPercentage: 0.05

  App\Domain\Quotation\Service\CampaignPolicy: '@App\Domain\Quotation\Service\FixedDiscountCampaignPolicy'

  # Proveedores in-memory
  App\Infrastructure\Quotation\Provider\ProviderAInMemoryAdapter:
    tags: ["app.quotation_provider"]

  App\Infrastructure\Quotation\Provider\ProviderBInMemoryAdapter:
    tags: ["app.quotation_provider"]

  # Use case
  App\Application\Quotation\UseCase\CalculateQuotesUseCase:
    arguments:
      $providers: !tagged_iterator "app.quotation_provider"
      # $campaignPolicy se autowirea desde la interfaz CampaignPolicy
